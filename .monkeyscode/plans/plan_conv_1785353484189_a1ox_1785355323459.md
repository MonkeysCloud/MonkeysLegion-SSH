# Fix Plan: Security, Bugs, and Edge Cases

## Phase 1: Security Fixes (S1, S2, S3)

- `stubs/ssh2.stub.php`: Add `ssh2_fingerprint()`, `ssh2_auth_agent()`, fingerprint constants
- `src/Stream/StreamHandler.php`: Add read timeout (`stream_set_timeout`), output size limit, check `timed_out`
- `src/Core/SSHConnection.php`: Add host key fingerprint verification, command timeout, random exit-code marker
- `src/Core/ConnectionBuilder.php`: Add `withFingerprint()`, profile `fingerprint` support

## Phase 2: Bug Fixes (B1, B2, B3, B4, B5)

- `src/Core/SSHManager.php`: Health-check cached connections, auto-reconnect dead ones
- `src/Core/SSHConnection.php`: Separate session-closer from stream-closer
- `src/Testing/FakeSSHConnection.php`: Override `sftp()` with clear error / injectable fake
- `src/Core/CommandPipeline.php`: Use local state copy in `run()`, don't mutate original
- `src/Facades/SSH.php`: Fix `@method sftp()` return type to `SFTPClient`

## Phase 3: Edge Case Fixes (E1–E10)

- `src/Core/ConnectionBuilder.php`: Port range validation, timeout validation, fix `is_numeric`, add agent auth
- `src/Authentication/AgentAuthentication.php`: New driver for `ssh2_auth_agent()`
- `src/Core/SSHConnection.php`: Random per-command exit marker
- `config/ssh.php` + `config/ssh.mlc`: Use `getenv()` instead of `$_ENV`, remove `agent` from comments (now supported)
- `src/SFTP/SFTPClient.php`: `download()` validates local parent directory
- `src/Core/SSHManager.php`: `register()` validates profile shape early
- `src/Core/PipelineResult.php`: Add `wasEmpty()` method

## Phase 4: Tests

- Update existing tests for new constructor signatures
- Add tests for all new behaviors (fingerprint, timeout, size limit, reconnect, agent auth, validation, etc.)

## Phase 5: Run Tests & Report Coverage

- Run PHPUnit, PHPStan
- Generate coverage report